-- Миграция 0003 — кэш названия видео в строке разбора.
-- Нужен для поиска по названию (вместе с текстом разбора) и для отображения
-- без обращения к YouTube на каждый показ.

alter table public.analyses
  add column if not exists title text;
